# E2E stabilization diagnosis — 2026-04-28

`docs/qa/playtest-report-2026-04-28.md` was not present in this checkout, so diagnosis used `playwright-report/.last-run.json`, `playwright-report/index.html`, and a fresh `npx playwright test --reporter=list` run.

## Cause classes
- A: selector/URL mismatch with current UI
- B: timing/race or flow synchronization
- C: test setup/isolation problem
- D: genuine product bug
- E: flaky environment

## Failed specs and remedies

| Spec / regression | Projects / failed IDs | Class | Diagnosis | Remedy |
|---|---:|---|---|---|
| `full-loop.spec.ts` final acceptance | desktop | B | Helper waited only for English `wins/draw` log text and clicked Flee after a resolved sub-area, which now returns to map or shows CTAs. | `waitForBattleResolution` now detects localized victory/defeat panels/logs; added `returnToTownFromCombat` that handles post-run CTA, Flee→map, and guarded `/town` fallback. |
| `mobile.spec.ts` responsive flow | desktop-driven 360/412 | B | Same localized combat-resolution and Flee→map flow mismatch. | Uses the shared robust combat resolution and return-to-town helper. |
| `save-load.spec.ts` persistence round trip | desktop | B/C | Same return-from-combat mismatch; state isolation also deleted the wrong IndexedDB database. | Uses `returnToTownFromCombat`; storage reset now deletes `D2H5TextGame`. |
| `playthrough.spec.ts` full screenshots flow | desktop/mobile | B/C | Started without clearing Dexie state, returned from combat via brittle Flee flow, and screenshot verification raced the screenshot-producing test across parallel workers/projects. | Starts with `clearGameStorage`, uses `returnToTownFromCombat`, marks the file serial, and verifies screenshots for the current project prefix. |
| `playthrough.spec.ts` screenshot verification | desktop/mobile | B | Verification ran before project screenshots existed and required the other project’s files. | Serial file execution; per-project 11-screenshot assertion (22 total across both projects). |
| `skills-screen.spec.ts` class skill rendering | desktop/mobile | A/C | Tests hard-coded `http://localhost:5173`, bypassing Playwright `baseURL`/webServer and reading stale dev-server state. | Uses shared `clearGameStorage`, `createCharacter`, and `navTo` helpers. |
| `playthrough/class-casts-skill.spec.ts` Bug #3 | desktop/mobile | C/B | Test bridge was absent when Playwright reused a stale preview; boosted-player helper silently no-op’d. | Playwright webServer no longer reuses stale servers and explicitly builds with `VITE_E2E=true`; boost waits for bridge and fails fast. |
| `playthrough/equip-picker.spec.ts` Bugs #9/#10 | desktop/mobile | C | `window.__GAME__` bridge missing under stale preview. | Deterministic E2E webServer and IndexedDB reset. |
| `playthrough/loot-drop-rate.spec.ts` Bug #4 | desktop | C/B | Boost/bridge no-op caused long fights/timeouts; storage leaked between tests. | Deterministic bridge plus stronger `boostPlayer`. |
| `playthrough/merc-fights.spec.ts` Bug #2 | desktop/mobile | C | Bridge missing, so merc seeding failed. | Deterministic bridge-enabled Playwright build. |
| `playthrough/monster-from-data.spec.ts` Bug #6 | desktop/mobile | C/B | Boost no-op and event-drain timeout, not data loading. | Deterministic bridge plus stronger boost. |
| `playthrough/no-gold-currency.spec.ts` Bug #12 | desktop/mobile | C/B | Boost no-op led to timeout before loot panel assertion. | Deterministic bridge plus stronger boost. |
| `playthrough/post-victory-cta.spec.ts` Bug #21 | desktop | C/B | Boost no-op prevented reaching a stable post-run panel. | Deterministic bridge plus stronger boost. |
| `playthrough/tooltip-edge-clip.spec.ts` Bugs #7/#8 | desktop/mobile | C | Bridge missing prevented item seeding. | Deterministic bridge-enabled Playwright build. |
| `playthrough/wave-progression.spec.ts` Bugs #5/#16 | desktop | C/B | Boost no-op and event-drain timeout prevented wave assertions. | Deterministic bridge plus stronger boost. |
| `playthrough/xp-after-fight.spec.ts` Bug #1 | desktop/mobile | C/B | Boost no-op prevented guaranteed win and XP polling timed out. | Deterministic bridge plus stronger boost. |
| `summon-verify.spec.ts` summon/timeline | desktop/mobile | A | Current UI exposes summon units via `data-kind="summon"` but not the legacy `summon-badge` test id. | Added neutral hidden `data-testid="summon-badge"` on summon unit cards. |

## Housekeeping
- Deleted stray `lint_output.txt` and removed the generated Playwright trace log from the working tree.
- Reverted unrelated generated screenshot diffs; no visual baseline update was intended by these fixes.
- Added React Router `future` flags to MemoryRouter-based Vitest renders to silence future-flag warnings.

## Product bugs
No new genuine product bugs (D) were found. The failures were setup, selector, and flow-synchronization issues in the E2E harness while preserving bug-fix regression intent #1–#21.
